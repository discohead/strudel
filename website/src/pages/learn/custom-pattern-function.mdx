---
title: Adding a Custom Pattern Function
layout: ../../layouts/MainLayout.astro
---

import { JsDoc } from '../../docs/JsDoc';

# Adding a Custom Pattern Function

This short guide demonstrates how you can add new pattern functions to Strudel. The example below implements a simple `mirror` function that concatenates a pattern with its reverse, creating a symmetrical cycle.

<JsDoc client:idle name="mirror" h={0} />
